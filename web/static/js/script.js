document.addEventListener('DOMContentLoaded', () => {
    // Apenas para garantir que o DOM está carregado, mesmo que não haja lógica específica aqui agora.
    // Este arquivo agora está vazio de funcionalidade de reconhecimento de fala.
});